# Local React app -> Cloudflare Worker dev server
VITE_API_BASE_URL=http://localhost:8787/api

# Production fallback used by the deployed frontend
# VITE_API_BASE_URL=https://newtronome-soundcloud-api.jsjweb0.workers.dev/api

# Set this as a Cloudflare Worker secret, not as a Vite/browser variable:
# npx wrangler secret put SOUND_CLOUD_CLIENT_ID
